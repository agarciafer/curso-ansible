---
- name: Configure an Ethernet connection with static IP
  hosts: 192.168.33.12
  become: true
  tasks:
  - include_role:
      name: linux-system-roles.network
    vars:
      network_connections:
        - name: eth2
          type: ethernet
          autoconnect: yes
          ip:
            dhcp4: yes
          state: up
